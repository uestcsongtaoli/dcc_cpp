# Build artifacts
build/
dcc_encrypt
*.zip

# Data files (large / generated)
data/table_data.csv

# Runtime files
server.log
.server.pid
/tmp/

# macOS
.DS_Store

# Claude — track settings but not conversation history
.claude/*
!.claude/settings.json

# autoresearch runtime (never commit)
results.tsv
run.log
